<Window x:Class="RIVS.ASAK.UIElements.Loading.SplashScreen"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d" 
        Height="100" Width="500" ResizeMode="NoResize" ShowInTaskbar="False"
        Topmost="True" WindowStyle="None" WindowStartupLocation="CenterScreen"
        BorderThickness="1" Background="#FF2D2D30" BorderBrush="#FF007ACC" Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="10*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Row="0" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="{Binding TitleText}" HorizontalAlignment="Center" 
                               Foreground="White" FontSize="16"/>
        </Grid>
        <Grid Row="1">
            <ProgressBar x:Name="ProgressBar" Height="20" Foreground="#FF007ACC"/>
        </Grid>
    </Grid>
</Window>
